plugin.tx_filetransfer {
  settings {
    template {
      includeUtilities = {$filetransfer.templates.includeUtilities}
    }

    upload {
      storage = {$filetransfer.upload.storageId}
      folder = {$filetransfer.upload.folder}
    }
  }
}
