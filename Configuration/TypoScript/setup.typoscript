plugin.tx_filetransfer {
  settings {
    template {
      includeUtilities = {$plugin.tx_filetransfer.settings.template.includeUtilities}
    }

    upload {
      storage = 1
      folder = filetransfer
    }
  }
}
